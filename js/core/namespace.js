(function () {
    window.NetworkShortcutApp = window.NetworkShortcutApp || {};

    const app = window.NetworkShortcutApp;
    app.core = app.core || {};
    app.features = app.features || {};
    app.apps = app.apps || {};
})();
